# Namespace config for the canary singleton, loaded by BaseAgent via the
# agent's explicit config_path. A fixed 'canary' namespace makes the singleton
# identifiable in the monitor and lets callers address it explicitly — every
# message to the agent carries namespace=canary. See site-canary
# docs/SWF_INTEGRATION.md.
[testbed]
namespace = "canary"
